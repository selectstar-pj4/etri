# API Configuration Template
# 이 파일을 복사하여 config.py로 이름을 변경하고 API 키를 입력하세요.
# config.py는 .gitignore에 포함되어 Git에 업로드되지 않습니다.

# OpenAI API Key (필수)
# OpenAI 웹사이트(https://platform.openai.com/api-keys)에서 발급받은 API 키를 입력하세요.
OPENAI_API_KEY = "your-openai-api-key-here"

# 기본 모델 설정 ('openai'만 사용)
DEFAULT_MODEL = 'openai'  # 기본값은 'openai'

# Google Drive 설정 (선택사항 - 사용하지 않으면 비워두세요)
GOOGLE_DRIVE_FOLDER_ID = ""  # Google Drive 폴더 ID
GOOGLE_CREDENTIALS_PATH = "credentials.json"  # 서비스 계정 JSON 키 파일 경로

